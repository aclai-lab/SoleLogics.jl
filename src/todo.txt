☐ Change Node name to ASTNode or something similar, maybe FNode (Formula node)

☐ Wrap a Logic in a Node. Concretize the following idea:

    tokens_of(::Type{Logic}) = Union{Letter, AbstractOperator}
    tokens_of(::Type{IntervalLogic}) = Union{Letter, AbstractIntervalOperator}
    tokens_of(::Type{L<:Logic}) = Union{alphbet(L), union_operators(L)...}

    mutable struct Node{T,L<:Logic}  #TODO: change name
        token::tokens_of(L)            # token (e.g., Proposition)
        ...
    end